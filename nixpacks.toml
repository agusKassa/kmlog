[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = [
  "npm install -g pnpm@11.0.8",
  "pnpm install --frozen-lockfile"
]

[phases.build]
cmds = ["pnpm --filter @kmlog/api build"]

[start]
cmd = "node apps/api/dist/main"
